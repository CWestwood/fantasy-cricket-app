# --- System Files ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Environment Variables (CRITICAL) ---
# Ignore all .env files in any directory
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Exception: Allow the example file so devs know what keys they need
!.env.example
client/.env
server/.env

# --- Dependencies ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-lock.yaml

# --- Client (React/Vite) ---
client/dist/
client/build/
client/coverage/
client/.eslintcache

# --- Supabase ---
# Local Supabase config/generated files that shouldn't be shared
supabase/.temp/
supabase/.branches/
supabase/functions/_shared/deno.lock  # Optional: Lock file for Deno can be ignored or committed depending on team preference

# --- Docker ---
# If you map volumes locally, you don't want to commit the data inside them
postgres_data/

# --- Misc ---
*.log
random_team_generator.py
team_players.csv
teams.csv
users.csv